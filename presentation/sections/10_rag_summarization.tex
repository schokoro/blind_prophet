\begin{frame}[plain]
  \begin{tikzpicture}[
    remember picture,
    overlay,
    x=0.1666667mm,
    y=-0.1666667mm,
    ragstep/.style={
      rounded corners=1.6mm,
      minimum height=8.5mm,
      inner xsep=2.6mm,
      inner ysep=1.7mm,
      align=center,
      font=\fontsize{8.7}{10.4}\selectfont\bfseries,
      text=BPText
    },
    ragnote/.style={
      anchor=west,
      inner sep=0pt,
      text=BPMuted,
      font=\fontsize{8}{9.6}\selectfont\itshape
    }
  ]
    \begin{scope}[shift={(current page.north west)}]

      \slideheader{От retrieved-новостей к структурированному саммари}

      % --- Left column: summarization pipeline ---
      \node[anchor=west, inner sep=0pt, text=BPMuted,
        font=\fontsize{8.4}{10}\selectfont\bfseries\scshape]
        at (64,110) {Пайплайн суммаризации};

      \node[ragstep, draw=BPSkyBorder, fill=BPSkyFill, minimum width=56mm, text=BPText]
        (queries) at (185,150) {9 тематических запросов};

      \node[ragstep, draw=BPSkyBorder, fill=BPSkyFill, minimum width=56mm, text=BPText]
        (topk) at (185,225) {топ-50 по каждой теме};

      \node[ragstep, draw=BPSkyBorder, fill=BPSkyFill, minimum width=56mm, text=BPText]
        (dedup) at (185,300) {дедупликация (cosine $\geq$ 0.9)};

      \node[ragstep, draw=BPSkyBorder, fill=BPSkyFill, minimum width=56mm, text=BPText]
        (theme) at (185,375) {тематические саммари $\times 9$};
      \node[ragnote] at (theme.east) [xshift=3mm] {параллельно};

      \node[ragstep, draw=BPNavy, fill=BPNavyFill, text=BPNavy,
        minimum width=56mm]
        (narrative) at (185,450) {итоговый нарратив};

      \draw[pipearrowmuted] (queries.south) -- (topk.north);
      \draw[pipearrowmuted] (topk.south)    -- (dedup.north);
      \draw[pipearrowmuted] (dedup.south)   -- (theme.north);
      \draw[pipearrowmuted] (theme.south)   -- (narrative.north);

      % --- Right column: summary output ---
      \node[anchor=west, inner sep=0pt, text=BPMuted,
        font=\fontsize{8.4}{10}\selectfont\bfseries\scshape]
        at (522,110) {Результат: нарративное саммари};

        \node[anchor=north west,
            rounded corners=1.4mm,
            draw=BPLine, fill=BPLight,
            line width=0.35pt,
            inner xsep=4mm, inner ysep=4mm,
            minimum height=55mm]
        (outbox) at (522,148) {%
          \parbox{62mm}{%
            \raggedright\fontsize{8}{11.5}\selectfont\textcolor{BPText}{%
              \textbf{Узел 1: Макроэкономическая ситуация и дилемма ДКП
              (дни 1–2)}\\[4pt]
              Годовая инфляция на середину периода составила 3,15\% при
              усилении проинфляционных факторов. Аналитики ЦБ отметили,
              что сдерживающие факторы могут быть временными; рост цен
              производителей потребительских товаров постепенно усиливается.%
            }%
          }%
        };

      % Arrow from narrative to output box
      \draw[->, draw=BPMuted, line width=0.4pt]
        (narrative.east) --
        node[above, font=\fontsize{7.5}{9}\selectfont\itshape, text=BPMuted]
          {800--1500 слов}
        (narrative.east -| outbox.west);

      \slidefooter{Сергей Устьянцев}{Извлечение сигнала инфляционных ожиданий
      из новостного потока с контролем утечки будущих данных}

    \end{scope}
  \end{tikzpicture}
\end{frame}